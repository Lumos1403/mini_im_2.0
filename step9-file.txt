**后端窗口**
cd D:\Study\Go_Project\mini_im_2.0-codex//对应根目录

$env:MYSQL_ROOT_PASSWORD="rootpass"
$env:MYSQL_DATABASE="go_im"
$env:MYSQL_USER="goim"
$env:MYSQL_PASSWORD="goimpass"

docker compose up -d mysql redis
如果容器已经存在，重新启动即可
docker start mini-im-mysql mini-im-redis


cd backend//后端目录

$env:MYSQL_DSN="goim:goimpass@tcp(127.0.0.1:3307)/go_im?charset=utf8mb4&parseTime=true&loc=Local"
$env:REDIS_ADDR="127.0.0.1:6379"
$env:JWT_ACCESS_SECRET="local-access-secret"
$env:JWT_REFRESH_SECRET="local-refresh-secret"

$env:AGENT_ENABLED="true"
$env:AGENT_API_BASE_URL="http://127.0.0.1:8100"
$env:AGENT_API_TIMEOUT_SECONDS="90"

cd D:\Study\Go_Project\mini_im_2.0-codex\backend
go run ./cmd/server


go run ./cmd/server

**前端窗口**//不同窗口该端口即可
cd D:\Study\Go_Project\mini_im_2.0xxx\frontend
npm.cmd run dev -- --port 5173 --strictPort



启动后访问：

```txt
http://localhost:5173
```

后端默认是：

```txt
http://localhost:8081
```
