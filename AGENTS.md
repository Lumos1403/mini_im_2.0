# AGENTS.md

## 1. 项目身份

本项目是一个基于 Go + Vue 的前后端分离即时通讯系统。

后端固定技术栈：

- Go
- Gin
- gorilla/websocket
- MySQL
- Redis
- JWT + Redis 双 Token
- bcrypt

前端固定技术栈：

- Vue 3
- Vite
- TypeScript
- Pinia
- Axios

部署方式：

- 本地开发
- Docker Compose
- 云服务器部署

除非用户明确要求，否则不得更换以上技术栈。

---

## 2. 核心业务规则

必须严格遵守以下业务规则：

1. 用户使用 `username + password + nickname` 注册。
2. `user_id` 使用雪花算法生成。
3. 密码必须使用 bcrypt 加密，禁止明文保存。
4. 登录使用 JWT + Redis 双 Token。
5. 每个用户注册后自动拥有默认 Agent 好友。
6. 好友添加必须由对方同意。
7. 删除好友后，双方好友列表都移除。
8. 删除好友后，对方聊天窗口显示系统提示。
9. 拉黑是单向拒收。
10. 被拉黑方发送消息时，前端显示红色感叹号。
11. 拉黑期间的消息不推送、不补发。
12. 消息类型至少支持 `text`、`emoji`、`file`、`system`。
13. 消息类型设计必须允许未来扩展。
14. 用户可以单向删除消息。
15. 用户可以清空自己的聊天记录。
16. 删除和清空只影响当前用户视角。
17. 5 分钟内可以撤回自己发送的消息。
18. 撤回后接收方无提示，但消息消失。
19. 撤回后发送方可在 5 分钟内重新编辑。
20. 文件最大 50MB，必须可配置。
21. 文件下载必须鉴权。
22. 群聊默认最大 50 人，必须可配置。
23. 群主可以设置管理员、禁言、解散群聊。
24. 群主和管理员可以审批入群申请。
25. 群主和管理员可以控制是否允许普通成员邀请他人。

---

## 3. 架构规则

后端必须采用分层架构：

```txt
handler -> service -> repository -> database/cache
```

目录职责：

handler：只负责参数解析、调用 service、返回响应。
service：负责业务逻辑。
repository：负责 MySQL 和 Redis 访问。
model：负责数据模型。
middleware：负责鉴权、限流、日志等中间件。
ws：负责 WebSocket 连接、心跳、事件分发。
pkg：放通用工具，例如 JWT、雪花算法、统一响应、错误码、日志。

必须遵守：

不允许把所有后端代码写进 main.go。
不允许在 handler 中写复杂业务逻辑。
不允许在 WebSocket 层直接操作复杂数据库逻辑。
不允许让前端代替后端做权限校验。
用户只能访问自己有权限的会话、消息、群聊和文件。
所有权限必须在服务端校验。
所有 HTTP 接口必须返回统一 JSON 格式。
所有错误必须使用统一错误码。
所有配置必须来自配置文件或环境变量。
JWT Secret、MySQL 密码、Redis 密码禁止硬编码。

---

## 4. 开发规则

每次只完成一个明确任务。
不允许一次性开发整个项目。
不允许擅自扩展任务范围。
不允许擅自删除已有功能。
不允许擅自更换技术栈。
不允许跳过文档中的业务规则。
不允许为了快速实现而绕过分层架构。
涉及数据库变更时，必须提供 migration。
涉及数据库变更时，必须同步更新数据库文档。
涉及 API 变更时，必须同步更新 API 文档。
涉及 WebSocket 事件变更时，必须同步更新 WebSocket 协议文档。
涉及权限、Token、消息删除、撤回、拉黑、群权限时，必须先说明实现方案，再写代码。
当前阶段不要引入 Kafka、Elasticsearch、微服务、Kubernetes 等复杂组件，除非用户明确要求。

---

## 5. 每次任务完成后必须输出

每次完成开发后，必须说明：

修改了哪些文件。
实现了什么功能。
如何运行。
如何测试。
是否有 migration。
是否更新了相关文档。
是否存在未完成事项。
是否发现潜在风险。

---

## 6. 当前开发优先级

第一优先级：

用户注册登录
JWT + Redis 双 Token
用户资料
好友系统
会话系统
WebSocket 基础
单聊文本消息
聊天记录分页

第二优先级：

文件消息
单条消息删除
清空聊天记录
5 分钟撤回
重新编辑
拉黑逻辑
消息搜索
文件搜索

第三优先级：

群聊
群权限
Docker 部署
高并发增强预留
