# AGENTS.md

## 项目定位

本项目是一个基于 Go 的前后端分离即时通讯系统。

后端技术栈：
- Go
- Gin
- gorilla/websocket
- MySQL
- Redis
- JWT + Redis 双 Token
- bcrypt

前端技术栈：
- Vue 3
- Vite
- TypeScript
- Pinia
- Axios

部署方式：
- 本地开发
- Docker Compose
- 云服务器部署

## 开发原则

1. 每次只完成一个明确任务。
2. 不允许擅自更换技术栈。
3. 不允许删除已有功能。
4. 不允许跳过文档中的业务规则。
5. 不允许把所有后端代码写在 main.go。
6. 后端必须按 handler / service / repository / model / middleware / ws 分层。
7. handler 只负责参数解析和响应，不写复杂业务逻辑。
8. service 负责业务逻辑。
9. repository 负责数据库和 Redis 访问。
10. WebSocket 层不能直接写复杂数据库逻辑。
11. 所有接口必须使用统一响应格式。
12. 所有错误必须使用统一错误码。
13. 涉及数据库变更时，必须同步 migration。
14. 涉及权限、Token、消息删除、撤回、拉黑、群权限时，必须先说明实现方案，再写代码。
15. 每次完成后必须说明：
    - 修改了哪些文件
    - 实现了什么功能
    - 如何运行
    - 如何测试
    - 还有哪些未完成事项

## 禁止行为

1. 不要一次性开发整个项目。
2. 不要为了快速实现而绕过分层架构。
3. 不要在前端伪造后端应该校验的权限。
4. 不要明文保存密码。
5. 不要让未登录用户访问私有文件。
6. 不要让用户访问不属于自己的会话、消息、群聊或文件。
7. 不要随意改动数据库字段含义。
8. 不要引入 Kafka、Elasticsearch、微服务等当前阶段未要求实现的复杂组件。

## 当前优先级

第一优先级：
- 用户注册登录
- JWT + Redis 双 Token
- 用户资料
- 好友系统
- 单聊
- WebSocket
- 消息入库
- 聊天记录分页

第二优先级：
- 文件消息
- 删除消息
- 清空聊天记录
- 撤回消息
- 拉黑

第三优先级：
- 群聊
- 群权限
- Docker 部署
- 高并发增强预留